import { Typography } from '@mui/material';

const ActiveBetsView = ({ bets }) => {

    return (
        <>
            <Typography pt={4} pb={4} variant='h4'>Active Bets</Typography>
        </>
    );
};

export default ActiveBetsView;