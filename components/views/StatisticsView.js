import { Typography } from '@mui/material';

const StatisticsView = ({ bets }) => {

    return (
        <>
            <Typography pt={4} pb={4} variant='h4'>Statistics</Typography>
        </>
    );
};

export default StatisticsView;